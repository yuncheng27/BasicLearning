#include <stdio.h>

//argc:整数，用来统计运行程序时送给main函数命令行参数的个数
//*argv[]:字符串指针数组，用来存放指向字符串参数的指针
//      argv[0]:指向程序运行的全路径名
//      argv[1]:指向在DOS命令行中执行程序名后的第一个字符串
//      argv[2]:指向程序名后的第二个字符串
//      ...
//      argv[argc]为NULL
//**env:字符串数组。env[ ]的每一个元素都包含ENVVAR=value形式的字符串。其中ENVVAR为环境变量，value 为ENVVAR的对应值
int main()  //main(int argc, char *argv[], char **env)
{
    
    return 0;
}
